const mustacheExpress = require('mustache-express');

/*const configMustache = (app) => {
    const engine = mustacheExpress();
    app.engine('mustache', engine);
    app.set('views', '../views');
    app.set('view engine', 'mustache');
}*/

//module.exports = configMustache;